<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is the line -->
    <item
        android:bottom="-10dp"
        android:right="-10dp"
        android:left="-10dp">
        <shape>
            <solid android:color="@color/transBlack" />
            <stroke
                android:width="1dp"
                android:color="@color/burnFaded" />
        </shape>
    </item>

</layer-list>