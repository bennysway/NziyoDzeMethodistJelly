<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:ads="http://schemas.android.com/tools"
    android:id="@+id/helpPopupBoxLayout"
    android:background="@color/fadedBlack">

    <ImageView
        android:id="@+id/helpDisplayIcon"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:scaleType="fitXY"
        android:background="@drawable/faded_black_center_large"
        app:srcCompat="@drawable/share_icon"
        android:alpha="1.0"
        android:layout_centerInParent="true" />
    <TextView
        android:text="Help"
        android:textColor="@color/white"
        android:shadowColor="@color/black"
        android:shadowDx="0.0"
        android:shadowDy="0.0"
        android:shadowRadius="14.0"
        android:padding="20dp"
        android:id="@+id/helpText"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/helpDisplayIcon"
        android:layout_centerHorizontal="true"
        android:textSize="40sp"
        />

</RelativeLayout>